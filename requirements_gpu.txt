# GPU-only requirements (Nvidia CUDA)
# Note: requirements.txt and PyTorch+CUDA are installed by setup_env.sh before this file
#
# Install order in setup_env.sh --gpu:
#   1. pip install -r requirements.txt
#   2. pip install torch==2.5.1 torchvision==0.20.1 --index-url .../cu124
#   3. pip install -r requirements_gpu.txt  (this file)
#   4. Flash-Attention wheel

# =============================================================================
# GPU acceleration (requires CUDA)
# =============================================================================
accelerate>=0.34.0
bitsandbytes>=0.44.0
xformers==0.0.29
triton

# =============================================================================
# LoRA / PEFT
# =============================================================================
peft>=0.13.0
trl>=0.12.0

# =============================================================================
# Unsloth (fast inference)
# =============================================================================
unsloth
unsloth_zoo
cut_cross_entropy
hf_transfer
